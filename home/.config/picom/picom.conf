backend = "glx";
vsync = true;

shadow = false;
fading = false;

corner-radius = 8;

animations = false;

rules: ({
  match = "window_type = 'dock' || window_type = 'desktop'";
  corner-radius = 0;
})
